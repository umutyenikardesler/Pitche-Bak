{
  "cli": {
    "version": ">= 16.19.3",
    "appVersionSource": "remote"
  },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal",
      "env": {
        "APP_VARIANT": "dev"
      },
      "ios": {
        "image": "macos-sequoia-15.3-xcode-16.2",
        "prebuildCommand": "printf 'export NODE_BINARY=$(command -v node)\\n' > ios/.xcode.env && printf 'export NODE_BINARY=$(command -v node)\\n' > ios/.xcode.env.local"
      }
    },
    "preview": {
      "distribution": "internal",
      "android": {
        "buildType": "apk"
      },
      "developmentClient": false,
      "ios": {
        "image": "macos-sequoia-15.3-xcode-16.2",
        "prebuildCommand": "printf 'export NODE_BINARY=$(command -v node)\\n' > ios/.xcode.env && printf 'export NODE_BINARY=$(command -v node)\\n' > ios/.xcode.env.local"
      }
    },
    "production": {
      "autoIncrement": true,
      "ios": {
        "image": "macos-sequoia-15.3-xcode-16.2",
        "prebuildCommand": "printf 'export NODE_BINARY=$(command -v node)\\n' > ios/.xcode.env && printf 'export NODE_BINARY=$(command -v node)\\n' > ios/.xcode.env.local"
      }
    }
  },
  "submit": {
    "production": {}
  }
}
